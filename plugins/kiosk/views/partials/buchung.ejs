<%
	var posneg = (buchung.amount() < 0) ? 'negative' : 'positive';
%>

<div class="row booking">
	<div class="span12">
		<a class="" href="/booking/<%= buchung.id() %>">
			<span class="id">Booking #<%= buchung.id() %></span>
			<span class="info">
				<span class="label"><%= buchung.type() %></span>
				<i class="icon-time"></i> <time title="<%- format.isodate(buchung.time()) %>"><%- format.isodate(buchung.time()) %></time>
				<span class="amount <%= posneg %>"><%= format.money(buchung.amount() / 100) %></span>
			</span>
		</a>
	</div>
</div>

<script type="text/javascript">
$(document).ready(function () {
	$('time').prettyDate();
	setInterval(function(){ $('time').prettyDate(); }, 5000);
});
</script>